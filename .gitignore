# Dépendances
# variantes d’images pré-cuites au build (artefact, jamais du code)
.img-cache/
node_modules/

# Secrets & config locale
.env

# Données locales (base SQLite + photos téléversées) — à sauvegarder séparément
data/
uploads/

# Divers
*.log
.DS_Store
demo-accounts.txt
